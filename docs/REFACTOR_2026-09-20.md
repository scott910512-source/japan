# UI 정리 · 버그 · 최적화 (2026-09-20)

9/16의 원칙(탭 넷 · 숫자는 한 곳에서 · 같은 곳으로 가는 길은 하나)을 이어받아
세 축으로 정리했다. 기능 추가는 없다. 학습 데이터·저장 열쇠·SRS 규칙·`settings.menus`
id·덱 id·`progress.*` 구조는 한 칸도 바꾸지 않았다.

기준선: `npm run test:logic` 32묶음 1,379개 통과, `npm run build` 성공 (index 1,406KB · CSS 122KB).

## 1. UI · 정보구조 (375px 실제 화면)

| # | 항목 | 상태 | 바꾼 파일 | 검사 | 비고 |
|---|---|---|---|---|---|
| 1 | 홈에 「오늘 복습 11개」가 두 번 | 완료 | `screens/Today.jsx` | today 95 · flow4 41 | 「이어서 공부하기」는 N3 코스 전용. 복습은 「오늘」 목록에서만. 밀린 복습도 그 목록의 한 줄(오늘 몫을 다 한 뒤에만) |
| 2 | N3 허브 0% 막대 열 줄 | 완료 | `screens/n3/N3Hub.jsx` · `styles/n3.css` | n3-ui 60 | 갈래 다섯 줄에 진도(위)·준비도(아래) 두 값. 전부 0%면 막대 대신 한 줄 안내. 준비도 카드 삭제, 헤더와 겹치던 본문 제목 삭제 |
| 3 | 내 학습 3칸 격자 네 번 · 기억 수준 눈금 | 완료 | `screens/Log.jsx` · `styles/overview.css` | flow4 · today | 오늘 3칸 삭제(홈이 보여 준다). 활동·전체를 한 카드에. `roundMax`에서 `fresh` 제외, 「아직 안 봄」은 숫자만 |
| 4 | 서브 화면 제목 두 번 | 완료 | `Basics` · `Grammar` · `SwissCourse` · `Jlpt` · `Settings` · `N3Hub` | menu-ui · swiss-ui · n3-ui | 헤더(`SUB_TITLES`)가 제목을 그리니 본문 `.navtitle` 삭제. JLPT 세트는 헤더(단어)와 다른 말이라 작은 구역 제목으로 |
| 5 | 「오늘」 숫자가 화면마다 다름 | 완료 | `screens/WordDeck.jsx` | menu-ui · jlpt-ui | 단어 화면의 판은 고른 레벨의 단어만 따로 짜는 것(`planDailySession`)이라 홈의 계획(`planStatus`)과 기준이 다르다. 홈과 같게 만들면 이 화면의 뜻(레벨을 골라 바로 시작)이 없어져서, 「이 레벨로 짜는 판 · 단어만」이라 부르고 홈과 별개라고 적었다. 내 학습의 「배정 19」는 3번에서 삭제 |
| 6 | 복습 탭 0짜리 줄 | 완료 | `screens/ReviewHub.jsx` | flow4 | 틀린 문제·취약 문법·문장 복습이 0이면 잠그고 「아직 없어요 —」로 통일(취약 단어 방식) |
| 7 | 같은 곳으로 가는 길 | 완료 | `Today.jsx` · `Log.jsx` · `App.jsx` | flow4 · today | N3 입구 넷 → 둘(홈 이어서 카드 · 학습 탭). 홈 준비도 줄은 보기만, 내 학습 카드는 통계만 보여 주고 누르면 학습 탭. **N3 허브 안의 오답노트·약점만 공부하기는 남겼다** — 코스 맥락(이 코스 안에서 틀린 것만, 코스의 꼭지로 바로 이어짐)이고, 복습 탭의 「틀린 문제」·「취약 문법」도 결국 같은 화면(오답노트·약점 판)을 연다. 화면은 하나, 길만 둘 |
| 8 | 단어 화면 밀도 | 완료 | `screens/WordDeck.jsx` | menu-ui | 분량 조절(설정 → 학습 설정에 있음)·진행률·전체 단어 수 삭제. 남은 것: 어떻게 끊을지 · 레벨 · 판 미리보기 · 시작 |
| 9 | 연습 5개 타일 빈칸 | 완료 | `styles/overview.css` | design | `.mtiles`를 flex로 — 한 줄 셋, 남는 것은 가운데. 340px 이하도 같은 폭 |

## 2. 버그

| # | 항목 | 상태 | 바꾼 파일 | 검사 | 비고 |
|---|---|---|---|---|---|
| B1 | 배지 `99+` vs 복습 탭 `11개` | **완료(확인됨)** | `lib/plan.js` · `App.jsx` · `Today.jsx` · `ReviewHub.jsx` | plan.mjs 58 (「배지에 밀린 것이 안 섞인다」) · flow4 41 | 배지가 오늘 몫 남은 수에 밀린 복습(`over`)까지 더하고 있었다. `reviewLeftOf(planStatus)` 한 함수를 셋이 같이 쓰고, 밀린 것은 복습 탭 안에서만 말한다 |
| B2 | 자정 넘김 | 재현·이상 없음 | — | plan.mjs(기존) | `useToday`가 자정에 깨우고 `ensurePlan` effect가 `today`에 걸려 새 날 계획을 짠다. 어제 세션은 `Today.resuming`·`startToday`·`guardDeck`이 모두 `session.date === today`를 보므로 「이어하기」가 아니라 「오늘 학습 시작」이 되고 새 큐를 짠다 |
| B3 | 서브 → 서브 → 탭 → 뒤로 | 재현·설계대로 | — | backnav 14 | 자리는 하나만 넣고(`history.state.jp`) 우리가 되돌리지 않는다는 설계. 탭 전환으로 sub가 닫힌 뒤 뒤로가기 한 번은 그 자리를 소비하며 아무 일도 안 하고, 한 번 더 누르면 앱을 나간다. 화면과 어긋나지 않는다 — 헛도는 한 번은 알고 감수한 것(useLayerNavigation.js 주석) |
| B4 | `progress.n3.summary`가 코스를 열 때만 갱신 | 재현·이상 없음 + 검사 추가 | `test/ui/flow4.js` | flow4 「코스에서 나오자마자 홈 N3 줄이 갱신된다」 | 요약 effect는 코스가 열려 있는 동안 `prog`·`ready`가 바뀔 때마다 돈다 — 오늘의 N3를 마치는 동안 이미 적히므로 홈으로 돌아오면 바로 바뀐다. 남는 경우는 코스 밖(오늘 학습)에서만 N3 단어를 복습한 날 — 다음에 코스를 열 때까지 준비도 숫자가 그만큼 뒤처진다. 코스 자료를 메인 번들에 안 싣는 대가라 그대로 둔다 |
| B5 | `usePersistAppData` 직렬화 비용 | 측정·변경 없음 | — | — | 회독 5,000장 `JSON.stringify` 5.3ms(Node), stats·plan·session은 0.03ms. 판정 한 번에 review 1회 + 나머지 3회 ≈ 6ms — 16ms 한 프레임 안이다. 묶어 쓰기(idle callback)는 앱을 바로 닫을 때 잃을 위험을 새로 만드니 하지 않았다. 회독이 만 장을 넘으면 다시 잰다 |
| B6 | TTS·STT 정리 | 재현·이상 없음 | — | listen-ui 75 · n3-exam-ui 26 | `speakJapanese`는 시작 전에 `stopSpeaking()`, 기기 음성은 `cancel()` 뒤 한 틱 떼고 말한다 — 빠르게 여러 번 눌러도 마지막 것만 난다. Listen·ListeningLesson은 unmount에 `stopSpeaking`. MicButton은 카드가 바뀔 때와 unmount에 녹음·인식을 정리한다 |
| B7 | 동기화 합치기 | 확인·변경 없음 | — | twosync 36 · mergevid 22 | `mergeReview`는 `lastSeen`이 나중인 쪽(`laterOf`)을 바탕으로 횟수는 큰 쪽. 같은 카드를 다른 날 판정하면 나중 날이 이긴다 — twosync가 본다 |
| B8 | 로그인 이메일 입력 | 확인·변경 없음 | — | — | `Account.jsx`에 이미 `type="email" inputMode="email" autoComplete="email"`, 비밀번호에 `autoComplete` |

## 3. 최적화

| # | 항목 | 상태 | 바꾼 파일 | 검사 | 비고 |
|---|---|---|---|---|---|
| O1 | 메인 번들 | 완료 | `lib/content.js`(새) · `lib/cards.js` · `App.jsx` | 전체 UI 검사 | 자료를 `import()`로. cards.js는 자료를 모르는 순수 함수만. `mergeUnique`는 빌드 시점 합치기까지는 안 갔다 — 3,000개 한 번 도는 데 5ms 미만이라 |
| O2 | CSS | 완료 | `index.css` · `N3Course.jsx` | n3-ui · design | n3.css를 코스 청크와 같이. 다른 CSS(practice·media)는 여러 화면이 같이 써서 순서 규칙을 지키려고 그대로 뒀다. `legacy/`는 빌드에 안 들어간다 |
| O3 | 렌더마다 다시 세는 것 | 완료 | `App.jsx` · `StudyHub.jsx` · `Log.jsx` | 전체 UI 검사 | `summarize`·`roundSummary`·`weakCards`를 App이 `review`가 바뀔 때만 한 번 세서 내려 준다. WordDeck의 `summarize`는 8번에서 삭제 |
| O4 | 큰 파일 | 완료(셋) · 보류(하나) | `App.jsx` → `app/useStudyQueue.js` · `Study.jsx` → `screens/study/*` · `Settings.jsx` → `screens/settings/*` | 전체 UI 검사 | App 902→770줄, Study 717→652, Settings 929→421. **Videos.jsx(875줄)는 보류** — 유튜브 플레이어·자막·AI 설명이 한 상태 덩어리를 같이 써서 억지로 쪼개면 prop 스무 개를 넘기게 된다 |
| O5 | 폰트·아이콘 | 확인·변경 없음 | — | — | 아이콘은 inline SVG, 웹폰트는 안 쓴다 |

### 숫자

| 측정 | before | after |
|---|---|---|
| 첫 로딩 JS (index chunk) | 1,406 KB | 476 KB (단어 자료 766 KB · 상황 문장 163 KB는 제 청크, 사전 캐시) |
| 첫 로딩 CSS | 122 KB | 108 KB (+ N3 코스 15 KB는 코스를 열 때) |
| Lighthouse 모바일 성능 (로그인 화면, 로컬 미리보기, 1회) | 88 · FCP 2.9s · LCP 3.2s · TBT 40ms · SI 2.9s · 전송 383KiB | 88 · FCP 2.4s · LCP 3.4s · TBT 0ms · SI 2.4s · 전송 383KiB |
| 판정 한 번의 저장 직렬화 | ≈6 ms | 변경 없음 |

Lighthouse는 한 번씩만 쟀다 — 점수는 같고 첫 그리기(FCP)·막힘(TBT)이 줄었다. 전송량이 같은 건
자료 청크를 여전히 앱을 열자마자 받기 때문이다(로그인 화면 뒤에서). 껍데기가 먼저 뜨고 자료는
따로 파싱되니 메인 스레드가 안 막힌다는 게 이번 변화의 요지다. LCP 0.2초 차이는 측정 편차 안.

## 4. 의심 목록 (고치지 않은 것)

- 단어 화면(WordDeck)의 판은 홈의 계획과 다른 기준으로 짠다(5번). 화면에 밝혀 두었지만, 둘을 하나로 합칠지는 제품 판단이라 열어 둔다.
- N3 준비도는 코스 밖에서만 복습한 날은 다음에 코스를 열 때까지 뒤처진다(B4).
- Videos.jsx 분할(O4).
